# =============================================================
# BharatDoc-VLM — Production Dependencies
# =============================================================
# Core ML
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.36.0
peft>=0.7.0
datasets>=2.16.0
accelerate>=0.25.0
safetensors>=0.4.0

# Vision & OCR — Primary: PaddleOCR (Hindi + English natively)
paddlepaddle>=2.6.0
paddleocr>=2.7.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
pytesseract>=0.3.10          # Fallback OCR engine
imagehash>=4.3.1

# VLM Extraction — Qwen2.5-VL + Donut
qwen-vl-utils>=0.0.8
sentencepiece>=0.1.99

# Near-duplicate detection (MinHash LSH — replaces poorly-maintained simhash)
datasketch>=1.6.4

# Document generation
Jinja2>=3.1.2
reportlab>=4.0.0
WeasyPrint>=60.0

# API & Serving
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
python-multipart>=0.0.6

# Async task queue
celery[redis]>=5.3.0

# Data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
PyYAML>=6.0.1

# Experiment tracking
wandb>=0.16.0
mlflow>=2.9.0

# Vector search & embeddings
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2

# Monitoring
prometheus-client>=0.19.0

# Data versioning
dvc>=3.30.0

# Quantisation — NOTE: bitsandbytes requires CUDA.
# On CPU-only environments, quantize.py uses a stub implementation.
# For CPU-friendly quantised inference, use llama-cpp-python with GGUF models.
# bitsandbytes>=0.41.0  # Uncomment on CUDA-enabled systems
# llama-cpp-python>=0.2.0  # CPU-friendly alternative

# Streamlit apps
streamlit>=1.29.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0

# Utilities
numpy>=1.24.0
pandas>=2.1.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
rich>=13.7.0
