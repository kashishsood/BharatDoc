git reset --soft HEAD~1
git reset HEAD .
git add schemas/ requirements.txt
git commit -m "feat: define Pydantic schemas for Indian document types

- Add Aadhaar schema with 12-digit validation
- Add LIC policy schema with plan number validation
- Add GST invoice schema with GSTIN validation
- Add handwritten form schema
- Pin dependencies in requirements.txt"
git add data_pipeline/
git commit -m "feat: add synthetic data generation and augmentation pipeline

- Implement Aadhaar card generator with realistic layouts
- Add invoice generator with GST compliance
- Add LIC policy document generator
- Implement augmentation pipeline (rotation, blur, noise)
- Add data quality checks with MinHash deduplication"
git add training/
git commit -m "feat: add LoRA fine-tuning scripts for Donut, LayoutLMv3, TrOCR

- Implement LoRA fine-tuning (r=16, alpha=32)
- Add training scripts for Donut on Aadhaar/LIC
- Add LayoutLMv3 training for invoices
- Add TrOCR training for handwritten forms
- Configure W&B logging and checkpointing"
git add inference/
git commit -m "feat: add FastAPI inference server with batching and circuit breaker

- Implement FastAPI server with async endpoints
- Add dynamic batching for throughput optimization
- Implement circuit breaker pattern for fault tolerance
- Add model loading with fallback mechanisms
- Configure health checks and readiness probes"
git add router/ gateway/
git commit -m "feat: add CLIP classifier router and API gateway

- Implement CLIP-based document type classifier
- Add routing logic to specialist models
- Build API gateway with request orchestration
- Add document upload and validation
- Implement response formatting and error handling"
git add evaluation/ monitoring/
git commit -m "feat: add evaluation metrics, Prometheus monitoring, Grafana dashboard

- Implement field-level F1 score calculation
- Add document-level accuracy metrics
- Add slice-based evaluation (by doc type, quality)
- Integrate Prometheus metrics collection
- Add Grafana dashboard with latency histograms
- Configure alerting rules for SLA violations"
git add feedback_loop/ mlflow/
git commit -m "feat: add active learning feedback loop and MLflow model registry

- Implement failure flagging endpoint
- Add active learning sample selection
- Configure Label Studio integration
- Set up MLflow model registry
- Add model versioning and rollback capability
- Implement A/B testing framework"
git add apps/ docker/ tests/ Makefile README.md
git commit -m "feat: add Streamlit apps, Docker deployment, test suite, documentation

- Add 5 Streamlit demo applications
- Implement Docker Compose multi-service deployment
- Add comprehensive pytest test suite
- Create Makefile for build automation
- Write production-grade README with architecture
- Add quick start guide and deployment docs"
git push origin main --force
